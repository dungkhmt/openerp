const initialState = {
  menuItemList: [
    /*
    [
      '',
    ]
    */
  ],
  isFetching: false,
};

const getMenuReducer = (state = initialState, action) => {
  return {state};
};

export default getMenuReducer;
